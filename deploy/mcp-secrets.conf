# Shared secrets — copy to /etc/mnemo/mcp-secrets.conf and fill in values.
# Single-service vars belong in deploy/%p.conf instead.

MCP_AUTH_TOKEN=changeme_openssl_rand_hex_32
MNEMO_GATEWAY_JSON=/etc/mnemo/gateway.json
QDRANT_REMOTE_URL=http://qdrant.local:6333

MNEMO_CONVERSATION_COLLECTION="mnemo-conversation"
PRESENCE_EMBED_URL=http://embed.local:30800

TOOL_CACHE_COLLECTION="mnemo-tool-cache"
TOOL_CACHE_EMBED_URL=http://embed.local:30801

XAI_API_KEY=xai-your-key-here
# MNEMO_CONTINUITY_COLLECTION=